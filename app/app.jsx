const React = require('react');
const { render } = require('react-dom');
const MemorizationApp = require('./components/MemorizationApp');

render(<MemorizationApp />, document.getElementById('main'));
